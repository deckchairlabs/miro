# 🖼️ miro (WIP)

An [imgproxy](https://imgproxy.net/) like `Request -> Response` middleware and
WASM module for image manipulation.

## TODO

- Automatic format conversion detection based on accepts header
- Support AVIF encoding
- Support WebP encoding
- Support presets
